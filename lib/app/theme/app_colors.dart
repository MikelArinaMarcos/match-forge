import 'package:flutter/material.dart';

abstract final class AppColors {
  // Marca
  static const Color primary = Color(0xFF38B6FF);
  static const Color primaryDark = Color(0xFF1A8FD1);
  static const Color primarySoft = Color(0xFFD9F1FF);
  static const Color secondary = Color(0xFFFFDE59);

  // Superficies
  static const Color background = Color(0xFFF7F9FC);
  static const Color surface = Colors.white;

  // Texto
  static const Color textPrimary = Color(0xFF172033);
  static const Color textSecondary = Color(0xFF647084);

  // Estados
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF57C00);
  static const Color error = Color(0xFFC62828);
}